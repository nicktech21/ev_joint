const show = (message, type = "success") => {
    // type : "info"|"error"|"success"|"warning"
    console.log("TODO: if you are seeing this in console then implement it.");
};

export const toastHelpers = {
    show,
};
